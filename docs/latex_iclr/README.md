# GeoODE-KD ICLR manuscript

The manuscript is split into main sections, appendices, references, and the
official ICLR template files. Build it from this directory with:

```bash
make pdf
```

The compiled manuscript is written to `build/main.pdf`.
